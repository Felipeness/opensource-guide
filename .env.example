# Use the server-side proxy (enables GITHUB_TOKEN auth)
NEXT_PUBLIC_API_URL=/api/search

# GitHub token for higher rate limits (10 -> 30 req/min)
# Generate at: GitHub > Settings > Developer Settings > Personal Access Tokens
GITHUB_TOKEN=
